# ============================================
# Fix HTTP 406 Not Acceptable Error - /pawel/ only
# Root Cause: ModSecurity blocking requests with incomplete User-Agent strings
# NOTE: SecRuleEngine Off causes 500 error - not allowed in .htaccess on this host
# ============================================

# TEST: Verify .htaccess is being applied
<IfModule mod_headers.c>
    Header set X-DX-HTACCESS "pawel-on"
</IfModule>

# Disable content negotiation
Options -MultiViews

# Attempt to remove ModSecurity rules (may not work if host doesn't allow)
# If this doesn't work, contact hosting support to disable ModSecurity for /pawel/
<IfModule mod_security2.c>
    SecRuleRemoveById 1-999999
</IfModule>
<IfModule mod_security.c>
    SecRuleRemoveById 1-999999
</IfModule>
